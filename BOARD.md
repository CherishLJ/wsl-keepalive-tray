# DSH 任务看板 v0.2

入口：监控面板的「任务看板」按钮，或托盘右键「DSH 任务看板」。支持现有五套主题与高 DPI。

显示真实会话列表、标题/目录/ID 搜索、仅运行中筛选、目录分组、父子会话、更新时间、详情及待办步骤。空闲表示当前没有生成，不代表任务已完成。

## 会话管理

- **重命名**：修改所选 DSH 会话标题。
- **复制分支**：从已完成的轮次创建新分支，保留原会话；不会自动发送消息。
- **中止本轮**：仅在所选会话运行时可用；请求中止当前轮次，排队消息仍保留，DSH 服务继续运行。
- **在看板隐藏**：仅在本机看板隐藏所选会话及子会话，聊天记录保留，运行不受影响。后续子会话继承隐藏状态。
- **显示隐藏 / 恢复到看板**：显示带「已隐藏」标记的会话，选中后可恢复；恢复子会话也会显示其父级，以便访问。

当前安装的 DSH 0.1.5-rc.1 未发现公开的永久删除会话接口。原生归档接口也未发现配套恢复接口，因此本版使用可恢复的本地隐藏，不调用原生归档或直接删除会话文件。

隐藏记录保存在 `%LOCALAPPDATA%\WSLKeepAliveTray\hidden-sessions.json`，只存会话 ID，原子替换。记录损坏时禁用写入并提示，不覆盖原文件。

看板打开时每 5 秒读取本地 DSH API，隐藏窗口后停止轮询。服务不可用时保留列表并标记未更新，禁止重命名、复制和中止；本地隐藏和恢复仍可用。管理操作不会自动重试，超时后应先刷新或在 DSH 中检查，避免重复复制。

「打开 DSH」进入网页首页。暂不提供发送消息、审批、任务完成判定或跨会话任务组织。

## 验证与配置

原程序 19 项自测通过。管理请求使用模拟传输验证名称、参数、响应匹配、错误及不重试；隐藏/恢复、子会话继承和损坏记录保护使用独立临时文件测试。真实服务仅执行会话列表读取，未修改现有会话。五主题最小窗口布局、在线/离线按钮状态和详情显示检查通过。

源码：`src/TaskBoardForm.cs`、`src/BoardManagement.cs`；测试：`tests/BoardTests.cs`。

保留 DSH 开机启动和 `Restart=no`；本看板不负责保活，不自动启动 WSL 或 DSH。

名称仍保留「任务看板」。后续若增加真正的任务层，可让一个目标关联多个会话，以验收条件和交付物判断完成，独立于会话运行状态；本版不改动该模型。

[Service]
# WSL owns a read-only WSLInterop registration. systemd-binfmt still applies
# distro rules successfully, but exits 1 when its initial global flush cannot
# remove that protected entry. Treat that WSL-specific outcome as successful.
SuccessExitStatus=1
